# Credentials for the archived integrations in lib/integrations/.
# The current site doesn't use these — set them only if an integration is re-wired.

# GitHub REST API (profile + repo stats). A PAT with public read access is enough.
GITHUB_TOKEN=

# YouTube Data API v3 (channel snippet + statistics).
GOOGLE_YOUTUBE_KEY=
GOOGLE_YOUTUBE_CHANNEL_ID=
